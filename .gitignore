# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Environment
.env

# Data & indices (regenerated by the ingestion pipeline)
data/raw/
data/processed/
# Prebuilt retrieval stores are committed so `git clone` is self-contained for
# the Docker deployment (see DEPLOY.md). ~38 MB total; regenerable via ingestion.
# data/qdrant/
# lightrag_data/
# bm25_index/

# Evaluation output
eval/results.json

# Apptainer / SLURM artifacts
*.sif
slurm/*.out
slurm/*.err
slurm/logs/

# uv
uv.lock

# Ruflo / local agent runtime artifacts
ruvector.db
agentdb.rvf
agentdb.rvf.lock
.claude/
data/redis/
data/redis_eval/
